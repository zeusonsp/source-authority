import { PlaceholderPage } from "@/components/app/placeholder-page";

export const metadata = {
  title: "Relatórios",
};

export default function RelatoriosPage() {
  return (
    <PlaceholderPage
      title="Relatórios"
      badge="Em construção · Fase 4"
      description="Esta tela será implementada na Fase 4 do roadmap (analytics + dashboard rico). Vai conter relatórios mensais auto-gerados em PDF (cliques, alertas, comparação MoM) e gráficos de tendência."
    />
  );
}
